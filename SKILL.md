---
name: expert-research
description: |
  批量研究专家信息工作流。当用户需要根据专家姓名列表查找每位专家的
  所在单位、研究方向、联系方式（邮箱）及信息来源时使用此技能。
  适用于科技奖/基金评审专家名单处理、学术会议参会专家信息收集等场景。
  只要用户提到"专家信息"、"专家名单"、"评审专家"、"批量搜索专家"、
  "专家信息汇总"等关键词，或需要将姓名列表转为结构化的专家信息CSV，
  都应该触发此技能。
---

# 专家信息批量研究工作流

## 概述

给定一份专家姓名列表（通常来自科技奖/基金评审专家公示名单），通过多Agent并行Web搜索，
查找每位专家的所在单位、研究方向、联系方式（邮箱）及信息来源网址，最终汇总为CSV文件。

## 前置条件检查

开始前确认以下条件：

1. **姓名列表文件存在** — 每行一位专家姓名，可能为 `.docx`、`.txt` 或 `.md` 格式
2. **姓名格式理解** — 姓名可能带有括号限定条件，如 `王伟（化工）`、`GUMIN（上海理工）`、`马骏（儿科）`，
   括号内的限定条件对搜索精度至关重要，不可忽略
3. **历史结果检查** — 检查是否存在 `results_*.txt` 格式的历史结果文件（格式为 `姓名|单位|研究方向|联系方式|来源网址`），
   如有则需去重避免重复搜索

## 工作流步骤

### 第一步：确定研究范围

1. 扫描当前目录下所有 `results_*.txt` 文件，汇总已完成专家的姓名
2. 读取原始专家名单文件：
   - `.docx` 格式：使用 python-docx 或 pandoc 提取文本
   - `.txt` / `.md` 格式：直接读取
3. 比对两个集合，生成待研究专家名单
4. 将待研究名单写入 `names_complete.txt`，每行一位专家姓名

如果名单在 `.md` 文件中且带序号，可用以下命令清理：
```bash
sed 's/^[0-9]*\t//' 专家姓名.md | sed '/^$/d' > names_complete.txt
```

### 第二步：分批切割

将待研究名单切分为每批约 **30人** 的批次文件。这个数字来自实践经验：
每批30人的Agent约需4-8分钟完成，既能保证搜索质量，又能充分利用并行能力。

使用 `scripts/split_batches.js`：
```bash
node .claude/skills/expert-research/scripts/split_batches.js names_complete.txt 30
```

这会生成 `research_batch_01.txt`、`research_batch_02.txt` 等文件。
输出会打印：`Total names: N, Total batches: M`

### 第三步：并行启动研究Agent

这是工作流的核心步骤。**关键原则：**

- 每个批次分配一个后台Agent，独立完成该批所有专家的搜索
- 所有Agent在**同一轮**中一次性启动（系统自动排队，并发上限约8-10个）
- 每个Agent独立使用 WebSearch + WebFetch，Agent之间不相互依赖

**Agent提示词模板**（填入具体批次名单后使用）：

```
你是信息研究助手。研究以下N位上海市科学技术奖评审专家，查找每位的所在单位、
研究方向、联系方式（邮箱），附上信息来源网站。

**重要规则：**
1. 优先匹配理工科方向的同名专家
2. 有括号限定条件时必须优先匹配（如"王飞（土木）"→土木工程方向）
3. 严格审核信息来源，大学官方主页 > 百度百科 > 中国科学院/工程院页面 > 学术平台 > 其他
4. 无法找到时填写"未找到"
5. 用WebSearch搜索，用WebFetch获取详情

**输出格式（|分隔）：**
姓名|单位|研究方向|邮箱|来源网址

**待研究专家（N位）：**
[粘贴批次名单]

使用WebSearch逐一搜索每位专家（如"王晓东 教授 研究方向 单位"），
打开最相关的1-2个来源页面获取详细信息。最终输出所有N条结果。
找不到的写：姓名|未找到|未找到|未找到|未找到
```

为每个批次文件调用 `Agent` 工具，设置 `run_in_background: true`：

```
Agent(
  description: "Research batch XX experts",
  subagent_type: "general-purpose",
  run_in_background: true,
  prompt: [上述模板填充具体名单]
)
```

所有Agent在一次响应中全部启动，等待后台通知逐一完成。

### 第四步：收集并保存每批结果

每个Agent完成后，提取其输出中的纯 pipe-delimited 数据，保存为 `batch_XX_result.txt`。

**关键处理规则：**

- 去除 Agent 输出中可能出现的 markdown 加粗标记（`**...**`）
- 确保每行恰好5个 `|` 分隔字段
- 统一"未找到"的写法：所有变体（"未公开"、"未公开邮箱"、"未找到个人邮箱"等）统一为"未找到"
- **每批保存后校验**：确认保存的行数等于该批专家数，不匹配则检查是否遗漏

### 第五步：合并已有结果

如果存在之前已完成的批次结果（如 `results_page5_batch1.txt`），这些文件已是 `|` 分隔格式，
直接纳入后续合并步骤即可。

### 第六步：生成最终CSV

使用 `scripts/merge_to_csv.js` 将所有批次结果合并为CSV：

```bash
node .claude/skills/expert-research/scripts/merge_to_csv.js
```

该脚本会：
- 扫描所有 `batch_*_result.txt` 和 `results_*.txt` 文件
- 合并去重，处理CSV转义（逗号、引号、换行）
- 写入带 BOM 的 `专家信息汇总.csv`（BOM确保Excel直接打开不乱码）

### 第七步：质量校验

使用 `scripts/validate_results.js` 统计质量指标：

```bash
node .claude/skills/expert-research/scripts/validate_results.js 专家信息汇总.csv
```

输出示例：
```
Total rows: 500
未找到单位: 23 (4.6%)
未找到联系方式: 115 (23.0%)
找到单位率: 95.4%
找到联系方式率: 77.0%
```

## 搜索策略要点

这是影响搜索结果质量的关键。遵循以下优先级：

1. **中文姓名**：搜索 `"[姓名] [可能单位] 教授 研究方向"`
2. **英文/拼音姓名**：搜索 `"[NAME] university china professor"` 或 `"[NAME] 中国 大学"`
   - 英文名需反查中文名后再搜索，如 `GUANYIMIN → 关一民`
3. **有限定条件**：直接利用括号信息，如 `"顾敏 上海理工 教授"`
4. **来源优先级**：大学官方教师主页 > 百度百科 > 中国科学院/工程院页面 > 学术平台(x-mol等) > 其他
5. **同名处理**：优先选取理工科方向的专家；如无法确定，标注不确定性而不是随便选一个
6. **跨地区正常性**：评审专家可能来自外地（如北京专家评审上海项目），这是正常情况

## 质量标准

| 指标 | 目标 |
|------|------|
| 找到单位率 | ≥ 95% |
| 找到邮箱率 | ≥ 75%（部分专家邮箱不公开） |
| 完全未找到率 | ≤ 5% |

## 常见问题与对策

| 问题 | 对策 |
|------|------|
| Agent输出含markdown标记 | 保存前用脚本清理 `**` 等标记 |
| 邮箱变体写法不统一 | 统一替换为"未找到" |
| 来源URL与专家不匹配 | 提醒Agent确认URL对应到该专家 |
| 拼音名无法直接搜索 | 先反查中文名再搜索 |
| 忽略括号限定条件 | 在Agent提示词中强调限定条件优先级 |
| 批次行数不匹配 | 每批保存后立即校验行数 |

## 文件清单

| 文件 | 说明 |
|------|------|
| `names_complete.txt` | 待研究专家完整名单（第一步生成） |
| `research_batch_XX.txt` | 第XX批次待研究名单（第二步生成） |
| `batch_XX_result.txt` | 第XX批次研究结果（第四步生成） |
| `results_page*_batch*.txt` | 已有历史结果（输入，如有则直接纳入合并） |
| `专家信息汇总.csv` | 最终合并输出CSV（第六步生成） |

## 脚本说明

所有脚本位于 `.claude/skills/expert-research/scripts/` 目录下：

- `split_batches.js` — 将名单文件切分为指定大小的批次文件
- `merge_to_csv.js` — 合并所有批次结果为最终CSV（含BOM）
- `validate_results.js` — 统计汇总CSV的质量指标

脚本用法详见各步骤中的命令示例。
